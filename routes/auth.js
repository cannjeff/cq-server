var passport = require('passport');

var auth = function ( app ) {

	// app.get('/v1/auth/twitter', passport.authenticate('twitter'), function ( req, res ) {
	// 	/* This will redirect to Twitter.com, so this function will never be called */
	// });

	// app.get('/v1/auth/twitter/callback', passport.authenticate('twitter', {
	// 	successRedirect: '/',
	// 	failureRedirect: '/login'
	// }), function ( req, res ) {

	// });
};

module.exports = auth;
